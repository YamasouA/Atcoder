#include <iostream>
#include <vector>
#include <cmath>
#include <string>
#define rep(i, N) for(int i = 0; i < N; ++i)
using namespace std;
using cor = pair<int, int>;
typedef long long ll;
const ll mod = 1000000007;

int main() {
    int n;
    cin >> n;

    if (n < 0) {
        cout << '0' << endl;
    }
    else {
        cout << n << endl;
    }
}